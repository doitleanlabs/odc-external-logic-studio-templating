using {{ProjectName}}.interfaces;
using {{ProjectName}}.structure;

namespace {{ProjectName}}.action {
    public class {{ProjectName}} : Interface{{ProjectName}} {
        public {{ProjectName}}() {

        }

        {{#each Actions}}
        public {{Output}} {{Name}}({{{ActionDefinition}}}) {
            {{{Content}}}
        }

        {{/each}}
    }
}